DROP DATABASE IF EXISTS connections_db;
CREATE DATABASE connections_db;
USE connections_db;

-- 2 tables in connections_db
-- Users Table - id, name, password, createdAt, updatedAt
-- Connections Table - id, name, level, role, industry, comapny, cost, createdAt, updatedAt
-- NOTE:  id, createdAt, and updatedAt are automatically created